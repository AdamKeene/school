display:
updateRow usedRows and text array unsafe, added synchronized to methods that access updateRow which is all of them

cars:
carworld adds car with trafficcontroller controller, controller flips traffic to true then back to false and notifies all to activate waiting cars

hashmap:
exception in thread Printer
ConcurrentHashMap from textbook, added volatile bc invariant only involves concurrenthashmap, don't know if volatile is necessary
delete people just deletes now

vector:
just synchronizing get and delete functions

message queue:
LinkedBlockingQueue and queue operations, main is in messagequeue

display 2:
wrap add and delete in a binary sephamore which is for locks not counting sephamore for counting, 

bank:
deadlock happens when t1 locks acc1 and tries to lock acc2 while t2 locks acc2 and tries to lock acc1 causing circular wait, lock ordering
get first lock number first, waits always flow down

web server:
executor starts 13 thread pool fixed size for simplicity 13 bc nproc 12 cores + 1, executor. execute to pool executor assigns automatically,
currently closes connection immediately dunno if i was supposed to change that to keep alive
tested with curl -s http://localhost:8080/ > /dev/null & curl -s http://localhost:8080/ > /dev/null & wait

word counter:
concurrency errors, used concurrenthashmap, 