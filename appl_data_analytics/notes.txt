m1
1. bias-variance tradeoff, as the number of predictors increases the model becomes more flexible, meaning it can better fit the training data but at the risk of increasing variance and overfitting, so a more flexible model is better suited for datasets less prone to overfitting like larger sample sizes, fewer predictors, less noisy data, or nonlinear relationships, and inflexible models are better suited for small sample size more predictors noisy data and linear relationships
2. classification problems are classifying things, placing things into categories, regression problems are assigning a value to something, we're interested in inference when we want the factors which affect the outcome and understand the relationships of the predictors, and we're interested in prediction when we just want an accurate prediction.
4. read example titles
5. with regression problems overfitting looks like a more wobbly unstable line on the chart and with classification it's more wobbly boundaries, but the factors for choosing a type of model are the same, sample size, number of predictors, noise in the data, inference or prediction, linear relationship or not
6. parametric models assume a functional form and non-parametric models don't, having a function makes it easier to interpret and works well when we can find the function, but when the function is wrong it obviously hurts the performance, parametric models are more inflexible and the tradeoffs are similar with flexible and inflexible models and generally you take the same factors into consideration.
8. remove the titles, pairs: some have a clear relationship some don't, boxplot: out of state tuition is higher for private schools, yes/no: 78 elite schools 700 not, boxplot: elite schools have higher tuition, histograms: schools tend to have less than 5000 full time undergrads, out of state tuition is usually 5000 to 15000 and it kinda stays up past that, schools more often have a higher percentage of terminal degree staff than not, elite schools have a higher graduation rate and cost more
9. name is the only qualitative predictor, this is range sd, ([1:7] leaves out name, sapply gets 1 from 0), removing 10 to 85 gives us slightly different values but not too far off, charts: mpg correlates pretty strongly with weight, engine displacement, and hp, displacement and hp are somewhat loosely correlated because horsepower depends on rpms and bore size and stuff too it depends on what you're going for you got turbos and rotaries above the line and diesels below the line but as you can see it generally correlates, and most cars are in the bottom left because most cars want fuel efficiency and as we saw low horsepower and displacement correlate with high fuel efficiency, and now here if we extract cars made by gm we can see that general motors vehicles are not fuel efficient
10. chart: most categories are at least a little related to each other, some are very related. mostly everything in the data is related to crime in some way, hist: most crime rates are on the low end some aren't, there's a few high ones I wouldn't want to live in 381, hist: tax rates are usually 300 or 400 except the highest tax rates are all the same and well outside the normal range at almost 700, hist: pupil teacher ratios are mostly in the low 20s and inconsistent otherwise, these are the two areas tied for the lowest home value neither are that close to the worst crime neighborhoods though, in neighborhoods with more than 7 rooms per dwelling on average the stats look pretty normal even a little better than the average but when you go to 8 there's way fewer results and the stats are much worse
m2
why no discernable pattern in 8c?
1. the null in this case is no association with sales, rejecting the null hypothesis means rejecting the hypothesis that a relationship does not exist, the idea being just because we didn't find a relationship in this instance doesn't mean one doesn't exist, we can only prove when it does, we can reject the null hypothesis for TV and radio because we found a statistically significant relationship with sales, but we can't reject the null for newspaper because we didn't find that relationship
2. knn k nearest neighbors, classifying by a plurality vote of its neighbors to classify it based on it's k nearest neighbors, knn regression is repurposing this algorithm for smoothing by averaging the values of a value's k nearest neighbors
3. We find when we write out the model that there is a negative relationship between college education and starting salary, and by fixing GPA we find that the minimum GPA for high school students to have a higher starting salary than college students is 3.5, so answer 3 is correct. b is just plugging values into the equation, and for c the specific value of a coefficient doesn't represent its weight when the values in the data aren't all standardized in terms of range and distribution
8. there's predictably a strong negative correlation between horsepower and mpg, c for intervals the prediction interval is a prediction of one individual car with 98 hp and confidence interval is a predicted mean mpg of all cars with 98 hp, so confidence intervals include uncertainty in the regression line and prediction includes the randomness of an individual car, so the prediction interval is going to be a wider range usually. least square regression line is the best fit of a line which is found by optimizing for the lowest squared distances from the line. Since we can see a pattern in the residuals graphs the data is likely not linear, residuals being the error the model makes, positive indicating underpredicting and negative indicating overpredicting, so data shouldn't all deviate the same way like we see in the three graphs, they should cluster at the middle in rvf, line in q-q, flat line in s-l, and for rvl we want no extreme points
9. cor() makes correlation coefficients -1 to 1, the lm has a few significant values, log looks flattest, we want the most linear plot with more even distribution because we're doing a linear regression so we optimize for the straight line, here we get a modest improvement on our r^2, r^2 is variance we can explain using the variables
10. we have three significant variables, we get the values for the equation from the coefficients section in summary, doing it again with just the good predictors gives us effectively the same re, sults but with a smaller model as we can see from the adjusted R^2. This residuals chart is much closer to what we expect from a linear model than before, clusters where they should be and no discernable patterns, we can see a few outliers in the last plot but not too bad
15. this function applies lm() to each predictor to predict crim, plot: plot for chas isn't clearly showing a relationship, it's the only one from the data without an association, others don't look linear but there is an association, plot: different coefficients nox is all the way over there. this equation fits all the predictors in a cubic regression model and we get multiple significant variables, showing those variables have a nonlinear relationship
m3
1. this is the logistic function which is used for finding probabilities of binary responses, and when we rearrange it the odds of success are an exponential function of X
2. the first function selects for the highest probability of being the correct classification, and the second selects the class with the highest discriminant score which is a scoring method that uses the mean, shared variance, and class prior, which is like baseline statistics on the data. turns out they always select the same classification because after you drop terms that are always the same and log transform it you get the other equation
4. when you add dimensions to a model meaning adding predictors each data point gets farther apart which makes using the nearest neighbors more difficult
13. logistic regression is discriminative meaning it directly learns boundaries between classes, as opposed to LDA QDA Bayes which are generative meaning modeling how features are distributed in each class, and it produces a linear decision boundary meaning it uses probability thresholds, 
LDA uses discriminant function from question 2, generative model that uses a linear decision boundary
QDA same thing but quadratic boundary
Naive Bayes assumes once the class is found other features are unrelated, which is often untrue, still provides good results sometimes especially with poorly calibrated probabilities or other imperfect assumptions
m4
3. classification error is the error rate on the majority class, gini index measures impurity, entropy measures uncertainty, they all peak around .5, the plot is percentage of the main class and impurity, at .5 is the most uncertainty and the edges are the most certain, classification error is linear, it only looks at majority correctness so its less smooth, gini index is quadratic so its a parabola and its more sensitive, entropy is a log so its curved and particularly penalizes uncertainty more in near pure nodes per the steepness near the edges
6. split the tree until a condition is met like minimum split size or minimum improvement to rss, then trim because the fully grown tree is likely going to be too complex, eliminate branches that don't improve test error enough to justify the complexity
7. looking for lowest stable mse, ntree drops error rate quickly then plateaus, mtry is variables tried at each spit, more mtry gives less variance
8. bagging is bootstrap aggregating, generates new training sets by sampling the dataset, sampling is uniform and with replacement, goal is variance reduction, random forest is used to fight overfitting and bias, uses many trees from samples from the data

------------------------------------------------------------
https://github.com/danhalligan/ISLRv2-solutions/tree/main
https://www.statlearning.com/resources-first-edition

flexible models in parametric approaches fit many functional forms but require estimating more parameters leading to overfitting ie following noise too closely
non-parametric is cool because no explicit assumptions but requries more observations for accurate estimates
restrictive models are more interpretable for inference, easier to distinguish the relationship of fewer variables
sometimes we only care about prediction not interpretability, inflexible might still be better bc overfitting though

The colon (:) is used to indicate an interaction between two or more variables in model formula. The asterisk (*) is use to indicate all main effects and interactions among the variables that it joins. 

look up confidence vs prediction interval, r squared, p value

poly() is orthogonal polynomials, does the Y = β0 + β1X + β2X2 + β3X3 + ϵ formula by default

pk(x) is posterior probability, chance x comes from class k
spread (σ), averages (μ_k)

look up why knn was better on 3

4-6: 8.1 page 333 (344)

5:
svm equation is same thing as the textbook equation in support-vector form, svm finds the best possible dividing line
maximal margin hyperplane is largest gap between two classes separated by svm
